{
  "name": "mini_game_maze_3d",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git add dist && git commit -m 'update dist' && git subtree push --prefix dist origin gh-pages"
  },
    "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^5.4.19",
    "vite-plugin-gh-pages": "^1.0.1"
  }
}
  